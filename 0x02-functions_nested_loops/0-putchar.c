#include <unistd.h>

/**
 * _putchar - writes the character c to stdout
 *
 * Return: On Success 1
 * on error, -1 is returned
 */

int main(void){

	char ch[8] = "_putchar";
	int i = 0;

	while (i <= 8)
	{
		c = ch[i];
		_puchar(c);
		i++;

	}
	_putchar('\n');
	return (0);

	}
