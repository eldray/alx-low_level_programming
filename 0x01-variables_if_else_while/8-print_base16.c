#include <stdio.h>

/**
 * main - Prints numbers between 0 to 9 an letters between a and f.
 *
 * Retun: Always o (Success)
 */
int main(void)
{ 
	int i;
	for (i = 48; i < 58; i++)
	{
		putchar(i);
	}
	for (i = 97; i < 103; i++)
	{
		putchar 9=(i);
	}
	putchar('\n');
	return (0);
