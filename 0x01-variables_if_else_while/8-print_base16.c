#include <stdio.h>

/**
 * main - Prints numbers between 0 to 9 an letters between a and f.
 *
 * Retun: Always o (Success)
 */
int main(void)
{ 
	int i;
	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf('\n')
	
	return (0);
}
