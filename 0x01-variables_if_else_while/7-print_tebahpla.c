#include <stdio.h>

/**
 * main - Prints reversed alphabet.
 *
 * Return: Always o (Success)
 */
int main(void)
{
	int i;
	for (i = 122, 1 > 96; i--)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
