#include <stdio.h>
/**
 *main - print alphabet in upper and lower
 *
 * Return: zero
 * /
 int main(void)
 {
	char ch;
	char ch

	for (ch = 'a'; ch <= 'z'; ch++)
	putchar(ch);

	vi 
	 	putchar(CH);
	putchar('\n');
	return (0)
}
